
#include <stdio.h>

int main()
{
   int i;
    int numbers[10];
    printf("Insert 10 numbers\n");
    for(i=0; i<10; i++){
        scanf("%d",&numbers[i] );
    }
    for(i=9; i>=0; i--){
        printf("%d, ",numbers[i]);
    }
    return 0;
}

