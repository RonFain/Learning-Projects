
#include <stdio.h>

int main()
{
    int number[5];
    int numbers=0;
    for(int i=0; i<5; i++){
        scanf("%d",&number[i]);
    }
    for(int i=0; i<5; i++){
        if(number[i]%2 == 0){
            numbers++;
        }
    }
    printf("%d",numbers);
    return 0;
}
